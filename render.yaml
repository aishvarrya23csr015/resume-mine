services:
  - type: static
    name: resume-mine
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist
    routes:
      - path: /*
        methods:
          - GET
        dest: /index.html
